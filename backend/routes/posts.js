const router = require("express").Router();

router.get("/", (req, res) => {
  console.log("Posts route");
});

// Create a post
// Get a post
// Update a post
// Delete a post

module.exports = router;
